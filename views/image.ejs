<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= cleanName %> | VISE</title>
    <meta property="og:title" content="<%= cleanName %>">
    <meta property="og:description" content="Uploaded by @<%= user %> on vise.best">
    <meta property="og:image" content="<%= url %>">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary_large_image">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        :root { --bg:#09090b; --panel:#18181b; --border:#27272a; --txt:#a1a1aa; --acc:#fff; }
        * { margin:0; padding:0; box-sizing:border-box; }
        body { background:var(--bg); color:var(--acc); font-family:'Inter', sans-serif; height:100vh; display:flex; flex-direction:column; align-items:center; justify-content:center; overflow:hidden; }
        .c { max-width:90vw; max-height:90vh; text-align:center; display:flex; flex-direction:column; align-items:center; }
        .img-box {
            border:1px solid var(--border);
            border-radius:12px;
            padding:8px;
            background:var(--panel);
            box-shadow:0 30px 60px rgba(0,0,0,0.6);
            margin-bottom: 20px;
        }
        .img-box img { max-width:100%; max-height:70vh; border-radius:6px; display:block; }
        .meta { display:flex; flex-direction:column; align-items:center; gap:10px; }
        .badge {
            background:#111;
            border:1px solid var(--border);
            padding:8px 16px;
            border-radius:50px;
            font-size:0.8rem;
            color:var(--txt);
            display:flex;
            align-items:center;
            gap:8px;
        }
        .dot { width:6px; height:6px; background:#22c55e; border-radius:50%; }
        .username { color:#fff; font-weight:600; }
        .filename {
            color:var(--txt);
            text-decoration:none;
            font-family:monospace;
            font-size:0.9rem;
            border-bottom:1px dotted #333;
            padding-bottom:2px;
            transition:0.2s;
        }
        .filename:hover { color:#fff; border-color:#fff; }
    </style>
</head>
<body>
    <div class="c">
        <div class="img-box">
            <img src="<%= url %>" alt="Upload">
        </div>
        <div class="meta">
            <div class="badge">
                <div class="dot"></div>
                uploaded on vise.best by <span class="username">@<%= user %></span>
            </div>
            <a href="/" class="filename"><%= cleanName %></a>
        </div>
    </div>
</body>
</html>
